<%= form_for(@transport_card_type) do |f| %>
  <% if @transport_card_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@transport_card_type.errors.count, "error") %> prohibited this transport_card_type from being saved:</h2>

      <ul>
      <% @transport_card_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
